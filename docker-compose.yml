version: '3.8'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: worker_ai_prod
      POSTGRES_USER: worker_ai
      POSTGRES_PASSWORD: worker_ai_pass
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
